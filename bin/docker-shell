#!/bin/bash

# Note: Docker doesn't seem to be able to detect "ctrl-@" keypresses at all,
# so this effectively disables detaching (while still being a valid configuration).
docker exec -it --detach-keys "ctrl-@,ctrl-d" "$(docker-name "$@")" "$SHELL" -li
