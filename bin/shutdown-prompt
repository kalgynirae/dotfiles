#!/bin/bash

ACTIONS=(poweroff reboot suspend)
ACTION=$(printf '%s\n' "${ACTIONS[@]}" | wofi --dmenu)

if [[ -n $ACTION ]]; then
    echo >&2 systemctl "$ACTION"
    systemctl --quiet --no-block "$ACTION"
fi
