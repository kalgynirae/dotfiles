[Unit]
BindsTo=graphical-session.target
After=graphical-session.target

[Service]
ExecStart=/usr/bin/swayidle -w timeout 910 'swaylock -f' before-sleep 'swaylock -f'
Restart=on-failure
Slice=session.slice
